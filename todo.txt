- check wall collissions and falling zones

- game over event
- map_tools crate air support with mark und L_ID
- finish levels 3,4,5
- slipperness effect

- level shader, decoration
- warp room
- game options
- save game
- time trial: no live subtract, no bonus or platforms, relic times
- ui: relic time (blue,gold,platinum)

####
#	if LC.map_zone and c.intersects(LC.map_zone):
#		landing(c=c,e=terraincast(c.world_position,LC.map_zone,LC.map_height))
#		return
#	d=raycast(c.world_position,Vec3(0,1,0),distance=.5,ignore=[c,LC.shdw],debug=False)
#	dE=d.entity
#	if d and not (str(dE) in LC.item_lst or str(dE) in LC.ign_obj):
#		if (is_crate(dE) and not dE.vnum == 0) and not (is_crate(dE) and dE.vnum in [9,10,11] and dE.activ) and c.fall_time > .1:
#			crate_action(c=c,e=d.entity)
#			return
#		if is_enemie(dE):
#			jump_enemy(c=c,e=dE)
#		else:
#			landing(c=c,e=d.world_point.y)
#			obj_act(c=c,e=d.entity)
#		return
#	c.landed=False